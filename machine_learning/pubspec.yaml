name: machine_learning
description: A new Flutter project.
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.0.0-403.0.dev <4.0.0'

dependencies:
  csv: ^5.0.1
  flutter:
    sdk: flutter
  tflite: ^1.1.2


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  assets:
    - data/SolenoidData_Mat.txt
    - data/
  uses-material-design: true
  